require([
            'JBrowse/Browser',
            'WebApollo/JSONUtils'
        ],
        function(
            Browser,
            JSONUtils
        ) {

describe("JSONUtils", function() { 

});

});
